#ifndef _UTIL_H
#define _UTIL_H

// requires to include <stdbool.h>
char isPrime(long);

int arrayPeriod(int*, int);

void printBytes(void* , unsigned short);

// requires to include <stdbool.h>
bool isPalindrome(long);

#endif